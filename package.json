{
  "name": "json-excel",
  "version": "1.0.0",
  "description": "Convert json data to excel with nested excel header",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json"
  },
  "keywords": [
    "json-excel"
  ],
  "author": "Nikesh Makaju",
  "license": "ISC",
  "dependencies": {
    "@types/flat": "^5.0.2",
    "exceljs": "^4.3.0",
    "flat": "^5.0.2"
  },
  "devDependencies": {
    "prettier": "^2.4.1",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.4.3"
  },
  "files": [
    "lib/**/*"
  ]
}
